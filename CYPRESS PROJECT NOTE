new folder in VS->
npm -i init -y
npm install -D cypress
npx cypress open
npx cypress run

install extension-> ES6 mocha snippets


node modules->cypress.config.js->code->


const { defineConfig } = require("cypress");

module.exports = defineConfig({
  e2e: {
    setupNodeEvents(on, config) {
      // implement node event listeners here
      return config
    },
    specPattern:"(cypress->new folder->new file->copy relavent path here)"
  },
});

then run cypress

descandit



28/6/24
xpath-> relative type-> //tagname [@atrbt="value"]



(//*text()="Contact Us"])[2]


